services:
  db:
    image: pgvector/pgvector:pg18
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: mini_ERP_system
    volumes:
      - pgdata:/var/lib/postgresql

volumes:
  pgdata: