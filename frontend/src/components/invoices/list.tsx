// src/components/invoices/list.tsx
import './list.css';

export default function InvoicesList() {
  return (
    <div className="invoices-list">
      <h1>Invoices</h1>
      {/* TODO: table of uploaded invoices with status (uploaded / extracting / pending review / approved) */}
    </div>
  );
}